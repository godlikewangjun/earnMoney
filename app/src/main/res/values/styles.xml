<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorAccent</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="windowNoTitle">true</item>
        <item name="android:textColor">@color/text</item>
        <item name="android:textSize">@dimen/text_size</item>
        <item name="android:windowBackground">@color/white_back</item>
        <!--去掉状态栏-->
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">false</item>

        <!-- 允许使用transitions -->
        <item name="android:windowContentTransitions">true</item>
        <item name="android:launchMode">singleTask</item>
        <item name="android:resizeableActivity">true</item>

<!--        <item name="colorControlNormal">#e1e1e1</item>-->
<!--        <item name="colorControlActivated">#e1e1e1</item>-->
<!--        <item name="colorControlHighlight">#e1e1e1</item>-->
        <item name="android:screenOrientation">portrait</item>
<!--        <item name="android:background">@color/white</item>-->
    </style>

    <style name="AppSplash" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowFullscreen">false</item>
        <item name="windowNoTitle">true</item>

        <!--透明-->
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:launchMode">standard</item>
    </style>

    <style name="line2" parent="AppTheme">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:background">@color/div</item>
    </style>

    <style name="progressBarHorizontal_indeter" parent="android:Widget.ProgressBar.Horizontal">
        <item name="android:indeterminateOnly">false</item>
        <item name="android:indeterminateDrawable">@drawable/progress_red_bar</item>
        <item name="android:progressDrawable">@drawable/progress_red_bar</item><!-- progress_horizontal -->
        <item name="android:minHeight">5dip</item>
        <item name="android:maxHeight">5dip</item>
    </style>

    <style name="Read_BottomMenu">
        <item name="android:textColor">@color/text</item>
        <item name="android:textSize">@dimen/read_text_bottom</item>
    </style>

    <style name="BottomMenu_Button"
            parent="Read_BottomMenu">
        <item name="android:gravity">center_horizontal|bottom</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
    </style>

    <style name="home_tab" >
        <item name="android:gravity">center</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:drawablePadding">5dp</item>
        <item name="android:textColor">@color/selector_tab</item>
        <item name="android:textSize">13sp</item>
    </style>

</resources>
